json.extract! cutaway_page_version, :id, :name, :language_id, :header, :body, :index, :visible, :images, :cutaway_page_id, :route_link, :created_at, :updated_at
json.url cutaway_page_version_url(cutaway_page_version, format: :json)
